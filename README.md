# VladiksLED Debug Project

Простой проект для дебага LED ленты - белый огонек бегает от первого до последнего светодиода.
**Настроен для Wemos D1 Mini (ESP8266)**

## Возможности

- Один белый светодиод движется по ленте туда-обратно
- Мигание встроенным светодиодом платы (автоматический поиск правильного пина)
- Настраиваемые параметры: яркость, скорость, количество LED

## Настройки в коде

```cpp
#define LED_PIN       6           // Пин для подключения ленты (D6 на Wemos)
#define NUM_LEDS      50          // Количество светодиодов
#define BRIGHTNESS    100         // Яркость (0-255)
#define SPEED         50          // Скорость движения (мс)
```

**Пины Wemos D1 Mini для справки:**

- D0 = GPIO16
- D1 = GPIO5
- D2 = GPIO4
- D3 = GPIO0
- D4 = GPIO2 (встроенный LED)
- D5 = GPIO14
- D6 = GPIO12
- D7 = GPIO13
- D8 = GPIO15

---

## 🚀 Быстрый старт для новичков

### Шаг 1: Установка PlatformIO

**Вариант А - через VS Code (проще):**

1. Откройте VS Code
2. Нажмите на иконку расширений (слева)
3. Найдите "PlatformIO IDE"
4. Нажмите "Install"
5. Перезапустите VS Code

**Вариант Б - через командную строку:**

```bash
pip install platformio
```

### Шаг 2: Установка драйвера CH340

Wemos D1 Mini использует чип CH340 для USB.

**Проверка:** Подключите плату к USB. Откройте "Диспетчер устройств" → "Порты (COM и LPT)"

- Если видите "USB-SERIAL CH340" с COM портом - драйвер есть ✅
- Если устройство с желтым восклицательным знаком - нужен драйвер ❌

**Установка драйвера:**

- Скачать: https://sparks.gogo.co.nz/ch340.html
- Установить и перезагрузить компьютер

### Шаг 3: Подключение платы

1. Подключите Wemos D1 Mini к USB
2. Запомните номер COM порта (например, COM3)

### Шаг 4: Открытие проекта в VS Code

```bash
cd c:\Users\sorok\arduino\VladiksLED
code .
```

Или просто откройте папку `VladiksLED` через VS Code.

### Шаг 5: Сборка проекта

**Через VS Code:**

1. Внизу появится панель PlatformIO
2. Нажмите на иконку "галочка" (Build) ✓
3. Ждите... PlatformIO сам скачает:
   - Платформу ESP8266
   - Компилятор
   - Библиотеку FastLED
   - Всё остальное

**Через командную строку:**

```bash
cd c:\Users\sorok\arduino\VladiksLED
pio run
```

При первом запуске это займет 2-5 минут - качаются все зависимости.

### Шаг 6: Загрузка на плату

**Через VS Code:**

1. Нажмите на иконку "стрелка вправо" (Upload) →
2. Готово!

**Через командную строку:**

```bash
# Автоопределение порта
pio run -t upload

# Или укажите порт вручную
pio run -t upload --upload-port COM3
```

### Шаг 7: Проверка работы

Если всё OK:

- Встроенный синий LED на плате должен мигать
- Если подключена LED лента - белый огонек бегает по ней

---

## 🔧 Полезные команды

```bash
# Сборка проекта (без загрузки)
pio run

# Загрузка на плату
pio run -t upload

# Очистка (если что-то сломалось)
pio run -t clean

# Просмотр Serial Monitor
pio device monitor

# Список подключенных устройств
pio device list
```

## Подключение LED ленты

- **DIN (Data)** → D6 (GPIO12) на Wemos
- **+5V** → Внешний блок питания 5V
- **GND** → GND Wemos + GND блока питания (общий минус!)

⚠️ **Важно:** Не питайте длинную ленту от USB! Используйте отдельный БП 5V.

## Возможные проблемы

**"Device not found" при загрузке:**

- Проверьте драйвер CH340
- Попробуйте другой USB кабель
- Укажите COM порт вручную: `pio run -t upload --upload-port COM3`

**Не компилируется:**

```bash
pio run -t clean
pio run
```

**LED лента не работает:**

- Проверьте подключение (DIN → D6)
- Проверьте питание ленты
- В коде измените `LED_PIN` на нужный пин

## Библиотеки

- FastLED 3.6.0+ (устанавливается автоматически)
